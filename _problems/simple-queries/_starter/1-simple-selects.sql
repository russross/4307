--
-- Write a SELECT query after each description below that provides the
-- requested data. All queries below can be satisfied using only the "states"
-- table in the database.
--
-- The database schema is given in the file .a-schema.sql, or you can
-- ask the database to report the schema to you. To load an interactice SQL
-- shell, use:
--
--    make shell
--
-- Within the shell you can see the schema for the "states" table by typing:
--
--    .schema states
--
-- Try each query out interactively first, then copy it into this file.
--
-- Note: the expected output of each of the queries below is given in
--
--     outputs/1-simple-selects.expected
--
-- Some details are omitted from the instructions and you are expected to
-- figure them out by looking at the expected output.
--
-- For example, in the first query the expected output has a heading
-- of "states_count", but you will only know that by looking at the expected
-- output. To modify the default heading, you need to rename the result
-- relation using "AS states_count" or similar.
--
-- In other queries, the exact set of attributes that you need to return, the
-- order in which they should be presented, etc., may be omitted from the
-- instructions, and you should plan to look at the expected output to fill in
-- the details.
--

--
-- Find the number of states listed in the database.
--
-- Your query goes here (in the blank space after each prompt)


--
-- Find the total population of the country, i.e., the sum of the population of
-- each state.
--


--
-- Find the 10 states with the youngest median ages.
--


--
-- Find the 10 states with the oldest median ages.
--


--
-- Find the states where a minority of residents are white.
-- List the states from most populous to least populous.
--


--
-- Find the 10 states with the most expensive median house price.
--


--
-- Find the 10 states with the most expensive house prices compared to income,
-- i.e., that minimize the ratio of
-- median_household_income : owner_occupied_homes_median_value.
--
